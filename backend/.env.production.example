# ⚠️ NÃO COMMITE ESTE ARQUIVO
# Este é um exemplo de como configurar o .env para PRODUÇÃO
# Copie este arquivo e edite com suas credenciais REAIS

# Configurações do Servidor
NODE_ENV=production
PORT=3001

# URL do Frontend (para CORS) - ajuste para seu domínio
FRONTEND_URL=https://seu-dominio.com

# Firebase Configuration - OPÇÃO A (RECOMENDADA PARA PRODUÇÃO)
# Use arquivo serviceAccountKey.json salvo FORA do git
# Coloque o arquivo em: /var/www/site-adm-app/backend/config/serviceAccountKey.json
# O código vai procurar automaticamente
# Deixe esta linha comentada se usar arquivo:
# FIREBASE_SERVICE_ACCOUNT=...

# Firebase Storage Bucket
FIREBASE_STORAGE_BUCKET=registro-itec-dcbc4.appspot.com

# Configurações de Segurança
JWT_SECRET=use-um-secret-muito-longo-e-aleatorio-em-producao
CORS_ORIGINS=https://seu-dominio.com,https://www.seu-dominio.com
GOOGLE_APPLICATION_CREDENTIALS="config/serviceAccountKey.json"

# Configurações de Upload
MAX_FILE_SIZE=5242880
MAX_FILES=5

# ===============================================
# INSTRUÇÕES DE SETUP PARA PRODUÇÃO
# ===============================================
#
# 1. Gerar nova chave Firebase:
#    - Firebase Console > Project Settings > Service Accounts
#    - Clique "Generate new private key"
#    - Salve o JSON
#
# 2. NO VPS:
#    - Copie o arquivo JSON para: /var/www/site-adm-app/backend/config/serviceAccountKey.json
#    - Altere permissões: chmod 600 config/serviceAccountKey.json
#    - Não commite o arquivo (está em .gitignore)
#
# 3. Alternativa (usar variável de ambiente):
#    - No servidor, configure a variável FIREBASE_SERVICE_ACCOUNT
#    - export FIREBASE_SERVICE_ACCOUNT='{"type":"service_account",...}'
#
# 4. Teste a conexão:
#    - npm run dev
#    - curl http://localhost:3001/api/alunos
#
# ===============================================
