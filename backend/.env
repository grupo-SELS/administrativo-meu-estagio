# Configurações do Servidor
NODE_ENV=development
PORT=3001

# URL do Frontend (para CORS)
FRONTEND_URL=http://localhost:5173

# Firebase Configuration para projeto: registro-itec-dcbc4
# OPÇÃO A: Usar arquivo serviceAccountKey.json (recomendado para desenvolvimento)
# 1. Vá em: https://console.firebase.google.com/u/4/project/registro-itec-dcbc4/settings/serviceaccounts/adminsdk
# 2. Clique em "Gerar nova chave privada"
# 3. Salve o arquivo JSON como: config/serviceAccountKey.json
# 4. Deixe a linha abaixo comentada:
# FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"registro-itec-dcbc4",...}

# OPÇÃO B: Service Account inline (para produção/deploy)
# ⚠️ NUNCA commite a chave inline! Use arquivo ou variável de ambiente no VPS
# Para desenvolvimento local, use OPÇÃO A
# FIREBASE_SERVICE_ACCOUNT={"type":"service_account",...COLOQUE_SUA_CHAVE_AQUI...}

# Para VPS/Produção: Configure como variável de ambiente no sistema operacional
# Não use arquivo .env para credenciais sensíveis!

FIREBASE_STORAGE_BUCKET=registro-itec-dcbc4.appspot.com

JWT_SECRET=S]@{$HGqAz9wG}Q?MN_P6f@LV4S:fsH8
CORS_ORIGINS=http://localhost:5173,http://localhost:3000
GOOGLE_APPLICATION_CREDENTIALS="serviceAccountKey.json"

MAX_FILE_SIZE=5242880
MAX_FILES=5